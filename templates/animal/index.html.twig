{% extends 'base.html.twig' %}

{% block title %}Hello AnimalController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {# {% if animales is defined %} #}
        {{dump(animales)}}
        <ul>ANIMALES:
        {% for animal in animales %}
            {# <li>
                <ul>
                    <li>{{animal.id}}</li>
                    <li>{{animal.tipo}}</li>
                    <li>{{animal.color}}</li>
                    <li>{{animal.raza}}</li>
                </ul>
            </li> #}
            <li>{{animal.tipo}}</li>
        {% endfor %}
        </ul>
    {# {% endif %} #}

    {# {% if animal_concreto is defined %}
        {{dump(animal_concreto)}}
        <ul>ANIMAL CONCRETO:
            <li>{{animal_concreto.tipo}}</li>
        </ul>
    {% endif %}

    {% if animales_concretos is defined %}
        {{dump(animales_concretos)}}
        <ul>ANIMALES CONCRETOS:
        {% for animal_concreto in animales_concretos %}
            <li>{{animal_concreto.tipo}}</li>
        {% endfor %}
        </ul>
    {% endif %} #}

</div>
{% endblock %}
